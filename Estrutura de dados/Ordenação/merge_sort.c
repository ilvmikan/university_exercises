#include <stdio.h>

void merge_sort(int data, int size){
    int middle = sizeof(data) / 2;
    int right = 
    int left = 
}

int main(){
    int data[] = {54, 26, 93, 17, 77, 31, 44, 55};
    int size = sizeof(data) / sizeof(data[0]);

    merge_sort(data, size);

    return 0;
}
